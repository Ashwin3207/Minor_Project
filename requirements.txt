# Core Flask framework
Flask==3.0.3

# Database ORM
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.35

# PostgreSQL support (needed for online database)
psycopg2-binary==2.9.9

# Password hashing (used in auth)
Werkzeug==3.0.4

# For migrations (optional but highly recommended for production)
Flask-Migrate==4.0.7
alembic==1.13.3

# Development tools
python-dotenv==1.0.1               # to load .env files (useful for secrets)

# Optional - better debugging in development
Flask-DebugToolbar==0.15.0         # install only in dev (add to config if needed)

# For CSV export (already in standard library, but explicit for clarity)
# csv is built-in, no need to list

# Production / deployment helpers (optional)
gunicorn==23.0.0                   # WSGI server for production

# Minimal pinned versions for reproducibility (as of late 2025 / early 2026)