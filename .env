# .env
# =============================================
# NEVER commit this file to Git!
# Add .env to .gitignore
# =============================================

# === Flask / App Settings ===
FLASK_ENV=development
# Change to 'production' when deploying
# FLASK_ENV=production

SECRET_KEY=your-super-secret-key-change-this-1234567890abcdef
# Generate a strong one: python -c "import secrets; print(secrets.token_hex(32))"

# === Database ===
# For development (SQLite) - no need to change
# DATABASE_URL=sqlite:///tpc_portal.db

# For production (PostgreSQL example) - uncomment and fill when ready
# DATABASE_URL=postgresql://username:password@localhost:5432/tpc_database_name

# If using Render / Railway / Heroku → they provide DATABASE_URL automatically

# === Session / Security ===
SESSION_COOKIE_SECURE=false          # Set to true in production (HTTPS only)
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax

# === Optional - Email (if you add Flask-Mail later) ===
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=true
# MAIL_USERNAME=your.email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_DEFAULT_SENDER="TPC Portal <no-reply@yourcollege.edu.in>"

# === App-specific ===
APP_NAME=TPC Portal
ITEMS_PER_PAGE=10

# === Debug / Development only ===
DEBUG=true                           # Set to false in production
DEBUG_TB_ENABLED=true                # Flask-DebugToolbar (only in dev)

# =============================================
# End of .env example
# =============================================